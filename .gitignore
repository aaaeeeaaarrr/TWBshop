# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/
.env

# Secrets
secrets.py
*.env
.env.*

# Database
*.db
*.sqlite3

# Logs
logs/

# Photos (stored on server / recreated by bootstrap)
photos/

# Downloaded supplier price lists (server-only, can be large)
pricelists/

# Archived receipt/report photos pulled from Telegram (server-only, business data — never commit)
receipts_archive/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Bootstrap token (machine-local, never commit)
.bootstrap_token

# Claude Code local settings (machine-specific, not for sharing)
.claude/settings.local.json

# Per-worktree lane notes (each lane keeps its own; never merge-conflicts)
CLAUDE.local.md

# Lane-guard cross-lane override marker (per-worktree, deliberate, never commit)
.lane_ack

# Scratch diagnostic from the --push-secrets workflow (delete-after; never commit)
_keycheck.py

# Telethon session files (contain auth tokens — never commit)
*.session
*.session-journal
